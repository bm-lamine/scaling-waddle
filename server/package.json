{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "env:test": "bun run src/env.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "^2.2.2",
    "drizzle-orm": "^0.44.3",
    "hono": "^4.8.9",
    "postgres": "^3.4.7",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.4"
  }
}
